export default function MarketingPage() {
  return (
    <main className="mx-auto flex w-full max-w-2xl flex-col gap-6 px-4 py-10 sm:px-6">
      <h1 className="text-3xl font-semibold tracking-tight">Beer Scramble</h1>
      <p className="text-sm text-zinc-600 dark:text-zinc-300">
        Mobile-first tournament scoring for scramble golf with beer bonus and
        birdie debt rules.
      </p>
    </main>
  );
}
